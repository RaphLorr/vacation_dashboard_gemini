# Local development only - DO NOT commit to git

# 阿里云万维易源节假日API AppCode
# Get your AppCode from: https://market.aliyun.com/products/57126001/cmapi00055124.html
ALIYUN_HOLIDAY_APPCODE=your_aliyun_appcode

# WeChat Work (企业微信) API Configuration
# Get these values from: 企业微信管理后台 > 应用管理 > 自建应用

# 企业ID (corpid) - 在"我的企业"页面可以查看
WECOM_CORPID=your_corp_id

# 应用Secret (corpsecret) - 在应用详情页可以查看
WECOM_SECRET=your_app_secret

# 审批模板ID (可选，用于筛选特定的请假审批模板)
# 在"审批"应用 > 模板管理中查看
WECOM_LEAVE_TEMPLATE_ID=

# Sync Scheduler Configuration
# Enable/disable automatic sync (default: true)
AUTO_SYNC_ENABLED=true

# Sync interval in cron format (default: every 1 minute)
# Format: "minute hour day month weekday"
# Examples:
#   */1 * * * *  = Every 1 minute (default)
#   */5 * * * *  = Every 5 minutes
#   0 * * * *    = Every hour at minute 0
#   0 9-18 * * * = Every hour from 9am to 6pm
SYNC_INTERVAL=*/5 * * * *

# Status Check Configuration (for active approvals)
# Enable/disable status check for pending approvals (default: true)
STATUS_CHECK_ENABLED=true

# Status check interval in cron format (default: every 5 minutes)
# This checks pending approvals in active_approvals.json for status changes
# Format: "minute hour day month weekday"
# Examples:
#   */5 * * * *  = Every 5 minutes (default)
#   */10 * * * * = Every 10 minutes
#   */15 * * * * = Every 15 minutes
#   0 * * * *    = Every hour at minute 0
STATUS_CHECK_INTERVAL=*/5 * * * *

# ============================================
# WeChat Work OAuth Configuration
# ============================================

# OAuth Agent ID (get from 企业微信管理后台 > 应用管理 > 自建应用)
# Required for web OAuth login
WECOM_OAUTH_AGENTID=your_agent_id

# OAuth Callback URL
# Development:
WECOM_OAUTH_CALLBACK_URL=http://127.0.0.1:10890/auth/callback
# Production (example):
# WECOM_OAUTH_CALLBACK_URL=https://your-domain.com/auth/callback

# Session Configuration
# Session secret for cookie signing (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=generate_a_random_64_char_hex_string

# Session expiration in days (default: 7)
SESSION_MAX_AGE_DAYS=7

# Environment (development or production)
NODE_ENV=development

WECOM_CALLBACK_TOKEN=your_callback_token
WECOM_CALLBACK_ENCODING_AES_KEY=your_43_char_encoding_aes_key
